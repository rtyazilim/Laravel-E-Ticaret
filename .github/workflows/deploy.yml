name: Deploy Laravel Project

on:
  push:
    branches:
      - main

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.2'
        
    - name: 🔍 PHP Syntax Check
      run: find app config routes resources database -type f -name '*.php' -print0 | xargs -0 -n1 php -l 2>&1 | grep -v "No syntax errors" | grep -E "^.*error" && exit 1 || exit 0

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        server-dir: ${{ secrets.FTP_REMOTE_DIR }}
        protocol: ftps
        port: 21
        security: loose
        timeout: 1800000
        dangerous-clean-slate: false
        dry-run: false
        exclude: |
          **/.git*
          **/.git*/**
          **/node_modules/**
          **/vendor/**
          **/tests/**
          **/storage/logs/**
          **/storage/framework/cache/**
          **/.env*
